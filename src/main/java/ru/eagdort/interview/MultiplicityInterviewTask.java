package ru.eagdort.interview;

/**
 * Реализовать метод check(int num), который возвращает массив байтов.
 * Написать тесты для данного метода.
 *
 * Логика работы метода:
 * - Если num кратно 3, то метод возвращет массив байтов строки "three"
 * - Если num кратно 7, то метод возвращет массив байтов строки "seven"
 * - Если num кратно и 3, и 7, то метод возвращет массив байтов строки "three&seven"
 * - Если num не кратно ни 3, ни 7, то выкидывать IllegalArgumentException
 *
 */

public class MultiplicityInterviewTask {

    public static byte[] check(int num) {
    }
}
